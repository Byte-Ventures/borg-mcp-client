# borgmcp 3.15.1

- Restores the current client/server pairing by aligning with `borgmcp-shared` version `0.12.3`; fresh setup and `borg update` now select `borgmcp-server@0.20.0` instead of retaining the older `0.18.0` pairing.
- Prevents recurrence by refusing client release preparation when the current published server pins a different exact `borgmcp-shared` version.
