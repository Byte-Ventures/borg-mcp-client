# borgmcp 3.16.1

- Updates the exact client pairing to `borgmcp-shared@0.13.1` and `borgmcp-server@0.21.1` while retaining protocol v10.
- Carries the shipped coordination-template policy that drone rerouting or reassignment requires explicit human operator approval for the exact work item and recipient.
- Preserves the 3.16.0 document, structured-citation, and startup-recovery behavior unchanged.
