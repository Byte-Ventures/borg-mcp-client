# borgmcp 3.12.0

- Makes activity-log delivery retry-safe with stable append identities and explicit deduplication results.
- Starts newly attached sessions from the server-provided initial log cursor, avoiding replay of pre-attachment activity.
- Updates the client and server contract to protocol v9 through `borgmcp-shared@0.12.2`.
