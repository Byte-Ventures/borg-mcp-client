# borgmcp 4.0.1

- Aligns shared drone guidance with mandatory explicit `to` addressing: drones acknowledge actionable assignments or direct action requests only when the entry's `to` audience includes them, not every addressed entry or mention. The existing ~60-second `borg_ack` tool instruction remains unchanged.
