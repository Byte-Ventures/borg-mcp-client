# borgmcp 3.17.0

- Adds the read-only `borg_ack-status` tool for inspecting per-recipient acknowledgements and separate advisory claims without advancing unread cursors.
- Updates the exact client pairing to `borgmcp-shared@0.14.0`, `borgmcp-server@0.22.0`, and protocol v11.
