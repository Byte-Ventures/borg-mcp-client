# borgmcp 5.1.0

- Adds first-class local handover guidance for repositories without a hosted remote: commits are the handover artifacts, and reviews use local Git refs without push or fetch steps.

- Updates the lockfile from `fast-uri` 3.1.5 to 3.1.7 and `qs` 6.15.3 to 6.16.0, clearing the current high-severity dependency audit findings.
