# borgmcp 5.1.1

- Ensures the launcher, SessionStart hook, and MCP child resolve the same live seat for a worktree. Duplicate seat stores are repaired when ordering is available; unordered legacy duplicates refuse safely and direct the operator to `borg reset-local-connection`.
