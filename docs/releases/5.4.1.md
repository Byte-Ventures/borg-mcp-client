# borgmcp 5.4.1

- Pins `borgmcp-shared` 2.2.0 so the published client/server pair is compatible again: `borgmcp-server` 4.2.0 pins 2.2.0, and with `borgmcp` 5.4.0 still on 2.1.0 the update preflight refused the mismatched pair (`borg update` reported "published pair is incompatible" and attempted no mutation). No other changes.
