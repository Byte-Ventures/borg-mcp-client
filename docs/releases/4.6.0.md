# borgmcp 4.6.0

- Restores the advisory clone-family warning for public repositories and scopes it by the canonical repository origin, so a second clone of the same repository warns while a different repository in the same cube does not (#263).

- Repositories without a remote have no identity shared across clones, so separate clones of a local-only repository remain undetectable by this warning (#264).
