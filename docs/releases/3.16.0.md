# borgmcp 3.16.0

- Adds immutable cube documents for durable Markdown or plain-text content, including linear revisions, audited removal, and exact-id retrieval.
- Adds structured document citations to activity-log writes, reads, and real-time wake lines without inlining document bodies.
- Updates the strict local protocol pairing to `borgmcp-shared@0.13.0` and the published `borgmcp-server@0.21.0` document contract.
